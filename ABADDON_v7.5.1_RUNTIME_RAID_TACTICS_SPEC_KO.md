# ABADDON v7.5.1 부팅 복구·길드 레이드 전술실 명세

## 1. 문제 원인

`v600_game_center.py`는 모든 게임센터 기능 키가 메뉴 기능군에 정확히 한 번씩 연결됐는지 import 시점에 검사한다. 배포본에서 `guild_donate`와 `guild_upgrade`가 둘 이상의 기능군에 들어가면서 다음 조건이 발생했다.

```text
missing=[]
duplicated=['guild_donate', 'guild_upgrade']
unknown=[]
```

검사가 `RuntimeError`를 발생시켜 `apocalypse_bot.core.bot` import가 중단됐고, Render가 프로세스를 재시작하는 반복 상태에 들어갔다. Discord 접속이나 데이터 로드 이후의 문제가 아니라 **봇 시작 전 게임센터 메뉴 메타데이터 충돌**이다.

## 2. 정상 연결 규칙

- 기능 정의 키는 전역에서 유일해야 한다.
- 모든 정의 키는 메뉴 기능군에 정확히 한 번 연결돼야 한다.
- 정의되지 않은 키는 메뉴에 들어갈 수 없다.
- 한 선택 기능군은 Discord 제한에 맞춰 최대 25개다.
- 카테고리 키와 카테고리 내부 기능군 이름도 유일해야 한다.

v7.5.1 배포 상태:

| 항목 | 결과 |
|---|---:|
| 기능 정의 | 276 |
| 메뉴 연결 | 276 |
| 누락 | 0 |
| 중복 연결 | 0 |
| 미등록 키 | 0 |
| 선택 제한 초과 | 0 |
| `guild_donate` 위치 | `guild/organization` |
| `guild_upgrade` 위치 | `guild/organization` |

## 3. 운영 모드 자동 복구

운영 모드의 목적은 메뉴 메타데이터 하나 때문에 봇 전체가 종료되는 일을 막는 것이다.

1. 메뉴를 선언 순서대로 읽는다.
2. 유효한 첫 번째 기능 연결을 보존한다.
3. 뒤에 나온 같은 기능 연결은 메뉴에서만 제거한다.
4. 정의되지 않은 키는 메뉴 연결에서 제외한다.
5. 누락된 기능은 원래 카테고리의 `자동 복구 기능` 기능군에 최대 25개씩 배치한다.
6. 중복 기능군 이름은 안전한 접미사를 붙여 정규화한다.
7. 복구 후 전체 규칙을 다시 검사한다.
8. 실제 명령 함수, 길드 데이터, 사용자 데이터는 삭제하지 않는다.

복구가 불가능한 상태가 남으면 그때만 `RuntimeError`를 발생시킨다.

## 4. 엄격 검사 모드

스테이징 또는 CI에서 다음 환경변수를 사용한다.

```text
ABADDON_STRICT_MENU_VALIDATION=1
```

엄격 모드는 자동 복구하지 않고 `missing`, `duplicated`, `unknown`, 잘못된 카테고리, 중복 기능군 이름, 25개 초과 위치를 포함한 오류로 중단한다. 운영 기본값은 `0`이다.

## 5. 개인 레이드 전술 프리셋

사용자 데이터의 `guild_raid_preset`에 다음 값을 저장한다.

```json
{
  "tactic": "assault | support | medic",
  "part": "auto | armor | core | sac",
  "updated_at": "ISO-8601"
}
```

잘못된 구형 값은 조회 시 `assault`, `auto`로 정규화한다. `!길드레이드공격`에 인자가 없으면 프리셋을 사용한다. 사용자가 명시한 파괴 부위는 오류로 안내하지만, 프리셋에 저장된 부위가 파괴된 경우에는 자동 부위로 전환한다.

## 6. 레이드 준비 점검

`!길드레이드준비`는 다음 정보를 한 화면에 표시한다.

- 개인 프리셋
- 현재 추천 공격 부위
- 개인 전투력
- 남은 행동 대기시간
- 무기고·발전기·의무실 등의 길드 시설 보정

실제 공격이나 보상 정산은 수행하지 않는다.

## 7. 무상태 레이드 연습

`!길드레이드연습`은 실전 길드 객체를 `deepcopy`한 뒤 복사본에서만 주간 레이드와 공격 결과를 계산한다.

변경하지 않는 항목:

- 실제 보스와 부위 HP
- 실전 공격 쿨다운
- 참가자 피해·지원·의무 기여도
- 길드 토벌 횟수
- 사용자 재화와 보상
- 저장 파일

연습 결과에는 예상 본체 피해, 예상 부위 피해와 치명타 여부가 표시된다. 사용자·길드 조합별 10초 계산 보호 대기시간을 둔다.

## 8. 레이드 기록

`!길드레이드기록 [페이지]`는 현재 레이드와 보존된 과거 레이드에서 중복 ID를 제거한 뒤 최신순으로 표시한다.

- 페이지당 5개
- 주차, 보스명, 진행/토벌/만료 상태
- 누적 본체 피해
- 참가 인원

기존 거래·감사·레이드 기록 자동 삭제 금지 정책을 유지한다.

## 9. 데이터·폐기 정책

- 스키마 버전은 6으로 올리되 기존 길드 데이터는 제자리 확장한다.
- 기존 길드 기금과 통합 금고 동기화 정책을 유지한다.
- 사용자 프리셋은 신규 필드이며 기존 데이터 초기화가 없다.
- 명령어, 기능, 길드, 사용자, 기록, 이미지 폐기·삭제는 0건이다.
- 폐기 후보를 발견해도 관리자 승인 전 실제 변경하지 않는다.

## 10. 검수 구조

- 전체 Python 컴파일과 AST 파싱
- 명령 이름·별칭 신규 충돌 비교
- 게임센터 AST 1:1 연결 검사
- 가짜 Discord 모듈을 사용한 `v600_game_center` 실제 import
- 중복 연결을 강제로 주입한 운영 모드 자동 복구 검사
- 같은 오류를 엄격 모드에서 차단하는 검사
- 길드 명령 모의 등록과 두 번 등록 시 멱등성 검사
- 프리셋·연습·실전 공격·기록 조회 행동 검사
- 입금·임무 보상·출금 승인·레이드 보상 동시성 회귀 검사
- 기존 길드 데이터 이관·기록 보존 검사
- 스토리 순차 해금과 관리자 우회 회귀 검사
- 홈페이지 JavaScript·HTML·로컬 링크 검사
