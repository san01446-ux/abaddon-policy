# ABADDON v7.6.0 길드 협동 파견 상세 명세

## 1. 목적

개인용 턴제 `원정`과 겹치지 않는 길드 전용 협동 콘텐츠다. 길드원은 모집 단계에서 역할을 선택하고, 길드장·간부·Discord 관리자가 길드 금고 자원을 사용해 파견대를 출발시킨다. 실제 전투 입력 없이 시간이 흐른 뒤 결과를 정산하는 비동기 구조다.

## 2. 개인 원정과의 구분

| 항목 | 개인 원정 | 길드 협동 파견 |
|---|---|---|
| 주체 | 개인 캐릭터 | 같은 길드 참가자 |
| 진행 | 턴제 전투 행동 | 출발 후 복귀 시간 대기 |
| 비용 | 개인 스태미나·아이템 | 길드 통합 금고 |
| 보상 | 개인 즉시 보상 | 길드 공동 보상 + 개인 claim 보상 |
| 전투력 | 현재 행동마다 사용 | 출발 순간 참가자 스냅샷 |

기존 개인 원정 명령과 데이터는 변경하지 않는다.

## 3. 상태 구조

길드 레코드의 `dispatch` 아래에 저장한다.

- `planning`: 모집 중 지역, 개설자, 참가자와 역할
- `active`: 출발한 파견 ID, 지역, 시작/복귀 시각, 참가자 전투력 스냅샷, 비용, 난수 seed
- `history`: 완료된 전체 파견 기록. 자동 절삭 없음
- `pending_rewards`: 사용자별 미수령 개인 보상
- `claimed`: `파견ID:사용자ID` 형식의 수령 완료 키
- `stats`: 시작·정산·성공·실패·수령 횟수

24시간 이상 방치된 출발 전 모집만 비용 없이 자동 종료한다. 완료 기록은 시간이나 개수에 따라 삭제하지 않는다.

## 4. 진행 흐름

1. `!길드파견모집 지역`
   - 운영 권한 검사
   - 진행 중인 모집/파견 검사
   - 개설자를 선봉 역할로 자동 참가
2. `!길드파견참가 역할`
   - 같은 길드원만 참가
   - 다시 실행하면 역할 변경
3. `!길드파견출발`
   - 길드별 잠금 획득
   - 현재 길드원만 참가자로 유지
   - 길드 금고 비용 검사와 차감
   - 참가자 전투력·역할 스냅샷 저장
   - 고유 파견 ID와 결정론적 seed 생성
4. 복귀 시각 이후 `!길드파견정산`
   - 같은 seed로 결과 1회 계산
   - 길드 금고 공동 보상 지급
   - 참가자별 개인 보상 대기 행 생성
   - 완료 기록으로 이동 후 active 해제
5. `!길드파견보상`
   - 미수령 claim key만 합산 지급
   - 수령 완료 키를 같은 잠금 안에서 기록

## 5. 결과 계산

- 참가자 역할 보정이 적용된 전투력 합계
- 서로 다른 역할 수에 따른 다양성 보너스
- 발전기·무기고 시설의 지원/공격 보정
- 파견별 고정 seed에서 계산한 제한된 변동치
- 권장 전투력 대비 결과 점수 비율

판정은 실패, 성공, 대성공으로 구분한다. 의무 역할은 실패 보상 손실을 줄이고 보급 역할은 공동·개인 보상을 높인다. 의무실 보상 보정도 반영한다.

## 6. 동시성·중복 방지

- v7.5 길드 금고·레이드와 동일한 길드별 `asyncio.Lock` 공유
- 비용 검사와 차감 분리 금지
- active 존재 여부와 출발 생성 동일 잠금 처리
- 정산 결과 생성, 공동 보상 입금, history 이동 동일 잠금 처리
- 개인 수령 검사, 재화 지급, claim key 기록 동일 잠금 처리
- 고유 ID와 claim key 중복 검사

## 7. 창고 한도

공동 보상은 현재 창고 시설의 식량·자원 한도를 따른다. 보관 가능한 양만 입금하고 초과량을 정산 메시지에 표시한다. 초과량을 조용히 다른 재화로 변환하거나 사용자에게 중복 지급하지 않는다.

## 8. 관리자·검수

`!760안정화검수`는 다음을 읽기 전용으로 검사한다.

- active와 planning 동시 존재
- 완료 기록 ID 중복
- claimed key 중복
- 사용자별 pending reward key 중복
- 검사 과정의 수정·삭제 0건

## 9. 폐기 정책

v7.6.0은 기존 기능, 명령, 길드 데이터, 스토리 진행, 이미지 파일을 제거하지 않는다. 기능 정리나 삭제는 별도 목록을 관리자에게 제시하고 승인받은 뒤에만 진행한다.
