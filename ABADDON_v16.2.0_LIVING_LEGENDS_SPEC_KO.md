# ABADDON v16.2.0 — LIVING LEGENDS 완전판

## 목표
초기 기능을 삭제하지 않고 현재의 방대한 기능을 한 번의 화면 이동으로 찾고, 사용자가 하던 행동을 바로 이어가며, 개인 서사와 시각 결과를 축적한다. 한국어와 English UI는 같은 기능 수준으로 별도 유지한다.

## 통합 명령어 센터
- `!명령어` / `!help` 콜백을 페이지형 UI로 교체
- 5개 영역 버튼: 시작, 플레이, 세계, 소셜, 운영
- 영역별 카테고리를 24개 단위로 페이지 처리해 Discord 선택 메뉴 25개 제한을 넘지 않음
- 오늘, 채집, 게임, 도시·차원 빠른 이동 버튼
- 대표 명령과 전체 명령 토글
- 키워드 검색 유지
- 기존 카테고리와 기존 명령 삭제 없음

## 통합 채집센터
- 일반, 도시, 차원, 크루 공동, 동료 지원 채집
- `!채집`에서 선택 메뉴를 열고 `!도시채집`, `!차원채집`, `!크루채집`은 바로가기 유지
- 도시채집 바로가기도 통합 처리기로 연결해 채집 명령 간 쿨타임 우회를 차단
- 모드별 가방, 숙련도, 단계 연출과 PNG 결과 카드
- 기존 개인 채집·BLACK CITY 도시 채집 로직 재사용

## 살아 있는 전설
- 개인 연대기, 최고 기록, 운명 성향, 오늘의 선택형 사건
- 탈것 8종과 캐릭터 오라·포즈·배경
- 크루 합동기와 관리자 선택형 도시 침공·축제·엔딩 연출
- 문맥형 대화에 현재 추천 동선과 채집·도시·보스 후속 대화 연결

## 편의 기능
- 편의센터, 현재 진행 요약, 즐겨찾기 8개, 최근 명령 12개
- 초기 기능 보존 검수, 명령어 UI 검수, v16.2 통합 검수
- 최신 `!패치노트`와 `!테스트 상세` 동기화

## Discord UI 호환성
- 알림 채널·역할 선택 메뉴에서 버전별 비호환 가능성이 있는 `default_values` 생성자 인자 제거
- 현재 선택은 임베드에 계속 표시
- 상호작용은 실행자 전용이며 선택·버튼 콜백에서 즉시 응답 또는 defer

## 데이터 호환
- v15.0 이하 저장 네임스페이스 유지
- 새 데이터는 `living_legends_v1620` 아래 서버·사용자별 분리
- v15.0 기준 봇 파일 삭제 0개, 홈페이지 파일 삭제 0개
