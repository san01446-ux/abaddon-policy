# ABADDON v7.5.0 중복 기능·폐기 승인 검토서

## 결론

- 실제 폐기: **0건**
- 실제 비활성화: **0건**
- 봇·홈페이지 파일 삭제: **0개**
- 데이터 기록 자동 정리: **0건**

관리자 승인 전에는 아래 항목도 삭제하지 않는다.

## 겹쳐 보이지만 이번에 유지한 항목

### 1. `!길드기부`와 `!길드입금`

둘 다 식량을 길드에 넣을 수 있어 기능이 겹쳐 보인다. `!길드기부`는 기존 사용자 호환 명령이고, `!길드입금`은 식량과 다섯 자원을 지원하는 통합 금고 명령이다. 기존 자동화와 안내문을 깨지 않기 위해 둘 다 유지하고 같은 길드 상태로 연결했다.

**향후 정리 후보:** 기존 사용량과 안내문 전환이 확인된 후 `!길드기부`를 호환 별칭으로만 남길지 관리자 승인 필요.

### 2. `!길드강화`와 `!길드시설강화`

이름은 비슷하지만 역할이 다르다. `!길드강화`는 기존 길드 레벨을 올리고, `!길드시설강화`는 발전기·창고·의무실·무기고를 강화한다. 현재는 중복으로 판단하지 않고 모두 유지한다.

### 3. 구형 `fund`와 새 `vault.food`

같은 식량 기금을 나타내는 저장 키가 두 개다. 기존 버전과 롤백 호환을 위해 둘 다 유지하며, 마이그레이션 시 더 큰 값을 기준으로 동기화한다.

**향후 정리 후보:** 여러 차례 실서버 백업·복구 검증 후 `fund` 읽기 전용 전환 또는 제거를 검토할 수 있으나 반드시 관리자 사전 승인 필요.

### 4. 기존 길드 메뉴와 새 길드 전용 메뉴

명령어는 삭제하지 않았다. 게임센터에서 길드 관련 항목을 전용 `길드`·`길드 레이드` 분류로 이동해 같은 버튼이 여러 분류에 중복 노출되는 것만 정리했다.

### 5. `!길드레이드랭킹`과 `!길드종합랭킹`

전자는 한 주 레이드 개인 기여도, 후자는 길드 레벨·시설·레이드·누적 기여를 합산한 길드 순위다. 목적이 달라 모두 유지한다.

## 승인 없이 하지 않는 작업

- 구형 명령어 삭제 또는 별칭 해제
- 구형 데이터 키 삭제
- 휴면 길드 삭제
- 거래·감사·레이드 기록 개수 제한
- 미수령 보상 제거
- 이미지·홈페이지 역사 문서 삭제

다음 정리 패치가 필요할 경우, 먼저 대상·사용 영향·데이터 이관·복구 방법을 목록으로 제시하고 관리자 승인 후 별도 버전에서 처리한다.
