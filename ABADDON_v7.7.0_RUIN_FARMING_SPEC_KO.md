# ABADDON v7.7.0 폐허 파밍·생활 기술 명세

## 1. 설계 목표

기존 `채집`, `광산`, `벌목`, `땅파기`, 개인 `원정`, `길드파견`을 복제하지 않고 다음 순환을 만듭니다.

> 지역 선택 → 물자 회수 또는 현장 사건 → 보물·폐품 후속 처리 → 납품 → 연구

기존 시스템은 수정·삭제하지 않으며 v7.7은 `farming_v770` 전용 사용자 데이터만 추가합니다.

## 2. 파밍 지역

| 지역 | 입장 레벨 | 스태미나 | 재출발 준비 | 주요 성격 |
|---|---:|---:|---:|---|
| 버려진 대형마트 | 1 | 8 | 8분 | 식량·생활 물자 |
| 붕괴 주거구역 | 5 | 10 | 12분 | 고철·나무·약초·폐품 |
| 지하 화물역 | 15 | 13 | 18분 | 광석·보물 파편·봉인 물자 |
| 흑색 격리구역 | 30 | 16 | 25분 | 오염 표본·설계도 조각·미감정 보물 |

하루 파밍 기록은 16회이며 KST 날짜 변경 시 초기화됩니다. 보상 배정 수치는 사용자 응답·홈페이지·이 문서에 싣지 않습니다.

## 3. 인카운트

현장 사건은 사용자 데이터의 `pending_encounter`에 저장됩니다.

```text
id / region / seed / encounter_index / created_at / expires_at / stamina_cost
```

동일한 `seed + 선택`은 같은 결과를 만들며, 실제 지급은 사용자 잠금 안에서 `pending_encounter`를 비우는 시점에 한 번만 이루어집니다.

선택:
- `fight`: 전투력 기반 돌파
- `evade`: 안전 회수
- `rescue`: 구조·연구 중심
- `search`: 추가 탐색·부상 가능

신호가 만료되면 다음 v7.7 명령 실행 시 안전 회피로 자동 정산합니다.

## 4. 보물 연동

미감정 보물은 기존 `v610_digging_treasure`의 `pending` 보물함에 직접 연결합니다.

- 기존 `!보물함`, `!보물감정` 사용
- 기존 `PENDING_LIMIT` 준수
- 보물함이 가득 찬 경우 신규 항목을 억지로 추가하지 않고 보물 파편으로 대체
- 기존 감정사·매입·등급 로직 유지

## 5. 폐품 공방

`workshop.unidentified`와 `workshop.identified`를 분리합니다.

- 감정: 미감정 폐품의 종류·등급·수리비·분해 결과 확정
- 분해: 고철·폐허 회로·설계도 조각 회수
- 수리: 식량·고철 사용 후 완제품 가치 정산
- 기존 장착 장비 내구도·강화·수리와 데이터 경로 분리

## 6. 전파 해독

- 하루 5회
- 탐색 간 준비시간 5분
- 4지선다 패턴 퍼즐
- 버튼 또는 `!신호해독 번호`
- 대기 퍼즐은 저장되어 버튼이 사라져도 명령으로 처리
- 해독 기록은 자동 삭제하지 않음

## 7. 일일 계약

사용자 ID와 KST 날짜로 하루 3개 계약을 결정합니다.

- 계약 수락은 한 번에 하나
- 보유 자원 확인과 차감, 보상 지급, 완료 키 저장을 동일 잠금에서 처리
- 완료 키가 존재하면 반복 납품 차단
- 자원 시장의 즉시 판매와 데이터·명령 흐름 분리

## 8. 연구

| 기술 | 주요 효과 |
|---|---|
| 현장 배급 최적화 | 파밍 스태미나 부담 완화 |
| 폐품 회수 공정 | 폐품 처리 재료 회수 개선 |
| 주파수 잡음 필터 | 전파 해독 연구 자료 개선 |
| 생존 계약망 | 납품 정산 조건 개선 |

연구는 연구 자료와 설계도 조각을 사용하고 완료 시간을 저장합니다.

## 9. 동시성·재접속

- 사용자별 `asyncio.Lock`
- 고유 파밍/인카운트/폐품 ID
- 선택 결과 지급 후 pending 제거와 저장을 한 임계구역에서 수행
- 계약 완료 키와 폐품 목록 제거를 지급과 같은 임계구역에서 수행
- 버튼은 소유자 검증 후 실행
- 버튼이 만료돼도 prefix 명령으로 복구

## 10. 게임센터

생활 카테고리에 `ruin_farming` 기능군을 추가합니다.

- 기능 21개
- Discord 선택 메뉴 제한 이하
- 각 ActionSpec은 정확히 하나의 기능군에 연결
- 빠른 목적 `식량과 자원이 필요해요`에 파밍·계약을 연결

## 11. 데이터 보존

- 기존 사용자 키 삭제 없음
- 기존 명령 삭제 없음
- 기존 이미지 변경 없음
- 파밍·공방·신호·계약·연구 기록 자동 삭제 없음
- 폐기 후보는 관리자 승인 전 제거하지 않음
